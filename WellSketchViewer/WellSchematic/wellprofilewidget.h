#ifndef WELLPROFILEWIDGET_H
#define WELLPROFILEWIDGET_H

// #include "depthscalewidget.h"

// class WellProfileWidget : public QWidget
// {
//     Q_OBJECT

// public:
//     explicit WellProfileWidget(QWidget *parent = nullptr);
//     void setDepthValues(const QVector<double> &depths);
//     void updateFromTree(class WellSchematicTree *tree);

// protected:
//     void paintEvent(QPaintEvent *event) override;

// private:
//     void drawWellComponent(QPainter &painter, int &yPos, const QString &type,
//                            double top, double bottom, double od, double id);
//     QColor getComponentColor(const QString &type);

//     QVector<double> m_depths;
//     double m_maxDepth;
//     WellSchematicTree *m_wellTree;
// };

#endif // WELLPROFILEWIDGET_H
